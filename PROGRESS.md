# PROGRESS

- Task 11: DONE - `llm/interpreter.py` converts an original engineering instruction plus a concise, semantic, ID-free `FaceInventorySummary` into an ordered list of strictly validated intents. Each intent contains a JSON-native Task 6 `op_list`, exactly one typed BC/load payload, and a plain-language `target_description`; force, stress, and displacement values remain value-plus-unit strings validated through `ground/semantics.py` rather than converted in the interpreter. The only live provider boundary is the official OpenAI Python SDK: `OpenAIStructuredOutputTransport` calls the Responses API through `client.responses.parse(..., text_format=OpenAIWireInterpretation)` and accepts only `response.output_parsed`; configuration is injectable, `OPENAI_MODEL` controls the default model, and the single fallback is `gpt-5.6-sol`. The provider wire model uses OpenAI-supported nested `anyOf`, recursive references, and `additionalProperties=false` on every object; it contains no `oneOf`, discriminator, propertyNames, or entity-ID field. Parsed wire values are converted to JSON-native payloads and then must pass the unchanged strongly typed internal `Interpretation` discriminated unions, Task 6 operation schemas, and Task 7 unit validators before return. Pydantic v2 models use strict validation and `extra="forbid"`; a recursive pre-validation guard additionally rejects `entity_ids`, face/edge/node/element ID variants, NSET/ELSET fields or text, and ID-like text in any nested output. The allowed model-facing query vocabulary is derived from the actual Task 6 constructors; `adjacent_to` and `in_component` are deliberately excluded because they require resolved IDs, while unknown operations, invalid arguments, and unavailable labels are rejected. Malformed/unsafe/schema-invalid replies receive a sanitized deterministic re-ask, never the unsafe raw reply, and stop after the configured bounded retry count with `InterpreterError`; attempts/retries are recorded on successful results. `scripts/smoke_llm.py` uses `OPENAI_API_KEY` only from the environment, never prints it, and exits cleanly when it is absent. Review corrections: direct IDs embedded in prose are rejected as well as forbidden fields; the prompt vocabulary is constructor-derived and shares spatial predicates with the Task 6 engine; default `hole_groups` output remains directly consumable by Task 6; the provider migration uses OpenAI Responses structured output; and the first live smoke reached the API but exposed an unsupported Pydantic-generated `oneOf`, which is now isolated behind the provider-compatible wire schema with an exact SDK-schema regression test. Scope review found no Task 12 grounding/query execution/candidate scoring, Region creation, session mutation, viewer/highlight work, Task 13 validation/confirmation, or export code. Dependency review: official `openai` was added with explicit approval and the unused previous provider dependency was removed; the verified test environment used OpenAI SDK 2.46.0. Takeover evidence: clean `main` at exact Task 10 commit `889f2f547ef4d66ec783604e0df6d9ad4e51bf5c`, equal to `origin/main`; pre-change `.venv\Scripts\python.exe -m pytest tests/ -x -q` was 143 passed. Final mocked evidence: `.venv\Scripts\python.exe -m pytest tests/test_interpreter.py -x -q` -> 30 passed in 2.72s with no live OpenAI request; `.venv\Scripts\python.exe -m pytest tests/ -x -q` -> 173 passed in 5.92s. Both final runs emitted only the known pytest cache write warning caused by sandbox permissions. The post-fix live smoke was not run by CI or Codex; rerun it manually with the environment-configured key.

- Task 11 live smoke update: The user-run post-fix OpenAI Responses API smoke test succeeded with one intent; `op_list` contained `hole_groups` with `min_size=2` and `max_size=2`; the BC was `fixed_displacement` with `x`, `y`, and `z` components; `target_description` was `"the two bolt holes"`; and no entity IDs were emitted. This successful manual run supersedes the preceding rerun request; CI remains mocked and makes no live API calls.

- Task 10: DONE - `app/session.py` provides a deterministic, in-memory selection session keyed one-to-one by each uploaded model's content-addressed ID. Each session owns the current validated `SimulationIntent` draft plus deterministic per-region selected-entity and active-highlight state. The additive API is `GET /session/{id}/intent`, `PUT /session/{id}/intent`, `POST /session/{id}/confirm_region`, and `POST /session/{id}/reject_region`; the frozen Task 8/9 routes and request schemas were not changed. Region status remains in the existing Pydantic IR. Only `proposed` regions may transition to `confirmed` or `rejected`; rejected regions retain provenance in the draft, leave active selection/highlight state, and reopen through a corrected PUT as `proposed`. Confirmed/rejected status is server-managed and those regions cannot be mutated or removed through PUT. Export eligibility is computed solely by calling the existing `SimulationIntent.export_payload()` confirmation gate, so any proposed/rejected required region blocks eligibility and all-confirmed regions allow it. Errors are deterministic: unknown uploaded model/session 404, missing region 404, missing draft or invalid transition 409, and invalid IR/request payload 422. Review correction: protected server-managed regions against removal by a later PUT, closing a rejected-region export-gate bypass. Scope review found no Task 11 interpreter/NLP/ambiguity grounding, Task 13 validation/audit-panel work, solver export, evaluation cases, or frontend redesign. Takeover evidence: clean `main` at exact Task 9 commit `74356645262018cc86fbf9ec61587a733585679f`, equal to `origin/main`; pre-change `.venv\Scripts\python.exe -m pytest tests/ -x -q` was 134 passed. Final automated evidence: `.venv\Scripts\python.exe -m pytest tests/test_session.py -x -q` -> 9 passed in 1.87s; `.venv\Scripts\python.exe -m pytest tests/ -x -q` -> 143 passed in 3.73s. Both final runs emitted only the known pytest cache write warning caused by sandbox permissions.

- Task 9: DONE — FastAPI now serves `app/static/` as a zero-build vanilla-JavaScript three.js viewer. The frontend uploads through the frozen Task 8 `POST /models` contract, loads the singular frozen `GET /model/{id}/gltf` route, preserves per-face `face_{tag}` node names for raycasting, records clicks through `POST /select {entity_id}`, provides damped orbit/zoom controls and a camera-oriented axes triad, and receives transient (non-session) `POST /highlight {entity_ids, style}` commands through a live event stream. Highlight rendering covers confirmed (green), proposed (blue), candidate (amber), fixed boundary condition (red screen-space hatching), and load direction (purple face plus arrow glyph; optional additive vector). Manual browser DoD: `bracket.step` rendered with 12 selectable faces; drag orbit and mouse-wheel zoom worked; Uvicorn recorded `face_11` and `face_12`; all five curl-driven highlight styles displayed as specified. Screen capture: `docs/task9-viewer.mp4` (37,119,306 bytes). Automated evidence: 17/17 Task 9 tests and 134/134 total tests passed. Scope review found no Task 10 session state, IR drafts, confirmation/rejection workflows, or export behavior. Task 8 commit `5aa20d8` was verified present before work began, with a clean takeover worktree.

- Task 8: DONE — app/server.py provides deterministic content-addressed STEP/INP uploads, Task 2/5 inventory responses, and embedded-buffer glTF 2.0 tessellation with one name-addressable node per CAD face (or INP boundary facet group). The plural Task 8 routes and singular CLAUDE.md viewer-contract aliases share identical handlers; no Task 9 selection/highlight behavior was added. Tests cover STEP inventory parity, face_1..face_12 nodes, buffer/accessor integrity, deterministic output, INP upload/rendering, duplicate uploads, route aliases, malformed/empty/path-traversal/unsupported/invalid uploads, missing models, and invalid glTF inputs. 16/16 Task 8 tests, 117/117 total. Review corrections: Gmsh tessellation now uses a fixed random seed with its required tiny nonzero perturbation (zero fails on coincident projected points), and model-store directory creation is lazy so importing the app has no filesystem side effect.

- Task 7: DONE — ground/semantics.py is the single mm-N-MPa conversion and load-semantics layer (N/kN/MN, Pa/kPa/MPa/GPa, mm/m), distinguishes resultant force, pressure, traction, concentrated force, per-node totals, direction conventions, and gravity; every interpretation emits pending audit assumptions. The 20-case DoD table covers the sprint-goal 5 kN resultant, 2 MPa pressure, gravity -Z, and mixed-unit traps. 23/23 Task 7 tests, 101/101 total. Review correction: pressure now records the positive-into-surface sign convention explicitly.

- Task 6: DONE — ground/queries.py deterministic JSON query engine (surface/hole selectors, true-radius and sign-independent axis filters, radius+axis hole groups, spatial/area ranks, adjacency/components, labels, set composition, candidate scores + margins), 12/12 new tests, 78/78 total. Bolt-hole grounding returns {11,12} through the r5.5 + Z-axis parallel group; it deliberately does not use coaxiality. Query execution consumes precomputed CylinderRecords alongside FaceInventory because true radius/axis cannot be derived from raw face bboxes.

- Task 1: DONE — env gate ENV OK, 15/15 tests pass. Surprises: repo docs/fixtures live in sim_intent_starter/ (not workspace root); fixtures shipped in fixtures/, copied to tests/fixtures/ per plan; pytest.exe not on PATH (user-site install), use `python -m pytest`; sprint-goal example JSON predates rule-2 provenance fields, so the hand-authored example carries the instruction text verbatim in source_instruction with status=proposed.
- Task 3: DONE — geom/cylinders.py (curvature axis + Kasa circle fit, angular extent via max angular gap of sampled points), 11/11 new tests, 35/35 total. Ground truth confirmed exactly: holes {10,11,12}, fillet 3 = fillet_partial (quarter arc, r4, X-axis, length 120), bolt group {11,12} r5.5 Z, wall hole {10} r8 Y separate. Surprise: the fillet's outward normal ALSO points toward its axis, so the material-side/inward-normal test alone would classify it as a hole — the full-circle (angular extent) check is the discriminator that actually defuses the trap. Analyzer cross-checks circle-fit radius vs curvature radius at 1% rtol and raises on disagreement. Bolt holes are parallel, not coaxial; coaxial_groups on bracket = all singletons.
- Task 4: DONE — geom/labels.py (extreme-face labels, adjacency via shared perimeter edges, components, above/below + height/area ranks), 18/18 new tests, 53/53 total. Bracket labels: top=4 (wall top, beats base top 5), bottom=8, left=1, right=9, front=7, back=6; plate labels faces 1-6, adjacency of bracket connected with 12 nodes. Surprises: the wall hole's sampled seam-midpoint normal is exactly +Z, so label eligibility must filter on surface_type=="Plane", not on normal alignment; face 2 (wall front, y=70) also has normal -Y, so the front label must go to the farthest projection (face 7 at y=0), not the first aligned face; plate top/bottom areas are bitwise-equal from OCC, so largest_face/area ranks need an explicit lowest-tag tie-break to be deterministic (bracket centroid-z ties 1/9 and 7/11/12 are exact too — full height_rank order is asserted in tests).
- Task 5: DONE — geom/meshes.py (INP+STL ingestion via meshio, NSET/ELSET names as MeshRegions in native ids, boundary FacetGroups by 30-deg normal region-growing), 13/13 new tests, 66/66 total. Fixtures generated in test setup: gmsh meshes bracket.step, writes INP + full-boundary binary STL (tests/fixtures/ untouched). Surprises: meshio re-indexes nodes/elements 0-based and discards the deck's native numbering, so parse_inp recovers native ids with a light *NODE/*ELEMENT keyword scan (verified: block lengths must match meshio's cell blocks or it raises); gmsh writes no NSETs — INP writer emits only ELSETs from physical groups (plus auto SurfaceN/VolumeN sets) — so the test generator appends the NSET block by hand; with physical surface groups defined, gmsh's STL/INP writers save ONLY physical-group elements unless Mesh.SaveAll=1 (the probe STL silently contained just 3 faces); gmsh ASCII STL trips a numpy overflow RuntimeWarning in meshio's binary-size sniff, so fixtures use binary STL. gmsh's STL winding is outward-oriented (bottom facet group normal exactly -Z), and mesh bottom group area/centroid match CAD face 8 within 5%/2mm — the grounding analog works.
- Task 2: DONE — parser + inventory + sha256 cache, 24/24 tests pass. Surprises: gmsh getNormal already returns outward-oriented normals for these OCC solids (verified against all planar faces of both fixtures — do NOT flip by the oriented-boundary sign, it disagrees); cylinder seam edges appear twice in getBoundary output, deduped in FaceRecord.edge_tags.
